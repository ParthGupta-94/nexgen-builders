# Production build flag — ships the photos-only site (no 3D render).
# Next.js loads this automatically for `next build` (including on Vercel),
# so the deployed site is always the image-only build. To deploy the 3D
# version instead, set NEXT_PUBLIC_NO_3D=0 (or remove this file).
NEXT_PUBLIC_NO_3D=1
